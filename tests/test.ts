import './ItemsObjectStores/test';
import './BasicObjectStore.test';

test('', () => {});
// Empty test to make the file detected as being an
// unittest file (the actual tests are ran by the imports)